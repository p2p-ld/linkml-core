id: http://example.org/base
name: base
prefixes:
  base: http://example.org/base/
default_prefix: base
default_range: string

classes:
  BaseThing:
    slots:
      - shared_slot

slots:
  shared_slot:
    range: string
